---
phase: 06-android-audio-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified: []
autonomous: false
requirements: [AUD-01, AUD-02, AUD-03, AUD-04, TTS-04]
gap_closure: true

must_haves:
  truths:
    - "All 29 unit tests pass when executed via ./gradlew test (18 CaeAudioManagerTest + 11 TtsAudioPlayerTest)"
    - "Project compiles without errors via ./gradlew compileDebugKotlin"
  artifacts:
    - path: "smait-jackie-app/app/src/test/java/com/gow/smaitrobot/CaeAudioManagerTest.kt"
      provides: "18 passing unit tests for channel adapter, frame formats, DOA JSON"
      min_lines: 80
    - path: "smait-jackie-app/app/src/test/java/com/gow/smaitrobot/TtsAudioPlayerTest.kt"
      provides: "11 passing unit tests for binary frame routing and player lifecycle"
      min_lines: 40
  key_links:
    - from: "CaeAudioManagerTest.kt"
      to: "CaeAudioManager.kt"
      via: "companion object function calls (adapt8chTo6chCaeFormat, extract4chRaw, buildAudioFrame, buildRaw4chFrame, buildDoaJson)"
      pattern: "CaeAudioManager\\."
    - from: "TtsAudioPlayerTest.kt"
      to: "TtsAudioPlayer.kt"
      via: "injectable audioWriter lambda for JVM-safe testing"
      pattern: "TtsAudioPlayer"
---

<objective>
Run Gradle build and unit tests in the lab environment to close the one verification gap from Phase 6: tests are written but have never been executed because JDK is not installed on the home dev machine.

Purpose: Confirm that the 29 unit tests (Plans 01 and 02) actually compile and pass, not just look correct in code review.
Output: Green test results, verified compilation, gap closed.
</objective>

<execution_context>
@/home/gow/.claude/get-shit-done/workflows/execute-plan.md
@/home/gow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-android-audio-pipeline/06-VERIFICATION.md
@.planning/phases/06-android-audio-pipeline/06-01-SUMMARY.md
@.planning/phases/06-android-audio-pipeline/06-02-SUMMARY.md

NOTE: This plan operates on the ANDROID app repo at /home/gow/.openclaw/workspace/projects/smait-jackie-app/ — requires JDK installed (Android Studio bundles JBR, or install JDK 17+ manually).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Gradle compilation and unit tests</name>
  <files></files>
  <action>
    In the smait-jackie-app directory, run the Gradle build and test commands. This requires JDK to be available (either via Android Studio's bundled JBR or a standalone JDK 17+ installation).

    1. Verify JDK is available: `java -version` should report JDK 17+. If not available, this task cannot proceed — surface as a checkpoint:human-action for JDK installation.

    2. Run compilation check:
       ```
       cd /home/gow/.openclaw/workspace/projects/smait-jackie-app && ./gradlew compileDebugKotlin
       ```
       This verifies all Kotlin source files (CaeAudioManager.kt, TtsAudioPlayer.kt, MainActivity.kt) compile without errors.

    3. Run unit tests:
       ```
       cd /home/gow/.openclaw/workspace/projects/smait-jackie-app && ./gradlew test
       ```
       Expected: 29 tests pass (18 in CaeAudioManagerTest + 11 in TtsAudioPlayerTest).

    4. If any tests FAIL: Read the failure output carefully. The tests were code-reviewed and found structurally correct, so failures likely indicate:
       - Import resolution issues (missing test dependencies in build.gradle.kts)
       - Kotlin visibility modifiers blocking test access to companion object functions
       - Minor API mismatches between test expectations and implementation
       Fix the failing tests or production code as needed, then re-run until green.

    5. If compilation FAILS: Read error messages. Common causes:
       - Android SDK path not configured (set ANDROID_HOME or local.properties)
       - Missing SDK platform version (run sdkmanager to install)
       Fix configuration issues and retry.
  </action>
  <verify>
    <automated>cd /home/gow/.openclaw/workspace/projects/smait-jackie-app && ./gradlew test 2>&1 | tail -30</automated>
  </verify>
  <done>
    - ./gradlew compileDebugKotlin exits with BUILD SUCCESSFUL
    - ./gradlew test exits with BUILD SUCCESSFUL, all 29 tests pass
    - No test failures or compilation errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify test results</name>
  <action>
    Human reviews the Gradle test output from Task 1 to confirm all 29 Phase 6 unit tests passed.
  </action>
  <verify>User confirms test results are acceptable</verify>
  <done>All 29 tests confirmed passing by human review</done>
  <what-built>Gradle build and all 29 Phase 6 unit tests executed in lab environment</what-built>
  <how-to-verify>
    1. Review the test output from Task 1 — confirm 29 tests passed (18 CaeAudioManagerTest + 11 TtsAudioPlayerTest)
    2. If any tests required fixes in Task 1, review the changes to ensure they are correct
    3. Optionally run `cd /home/gow/.openclaw/workspace/projects/smait-jackie-app && ./gradlew test --info` for verbose output showing individual test names
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `cd /home/gow/.openclaw/workspace/projects/smait-jackie-app && ./gradlew compileDebugKotlin` — BUILD SUCCESSFUL
2. `cd /home/gow/.openclaw/workspace/projects/smait-jackie-app && ./gradlew test` — BUILD SUCCESSFUL, 29 tests pass
3. No test failures in CaeAudioManagerTest (18 tests) or TtsAudioPlayerTest (11 tests)
</verification>

<success_criteria>
- Gradle compilation succeeds for all Phase 6 Kotlin files
- All 29 unit tests execute and pass (not just code-reviewed)
- Phase 6 verification gap closed: "Unit tests verified to pass (compiled and executed)" truth becomes VERIFIED
</success_criteria>

<output>
After completion, create `.planning/phases/06-android-audio-pipeline/06-03-SUMMARY.md`
</output>
