---
phase: 12-android-app-rebuild-and-wie-theme-home
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/gradle/libs.versions.toml
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/settings.gradle.kts
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/build.gradle.kts
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/build.gradle.kts
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/JackieApplication.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/data/model/ThemeConfig.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/data/theme/ThemeRepository.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/ui/theme/AppTheme.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/ui/theme/WiEColors.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/data/model/ChatMessage.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/data/model/NavStatus.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/data/model/PoiItem.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/data/model/FeedbackData.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/assets/wie2026_theme.json
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/assets/default_theme.json
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/AndroidManifest.xml
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/test/java/com/gow/smaitrobot/ThemeRepositoryTest.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/test/java/com/gow/smaitrobot/WiEThemeTest.kt
autonomous: true
requirements: [APP-01, APP-02, APP-09, WIE-01]

must_haves:
  truths:
    - "App builds successfully with Jetpack Compose and Material 3"
    - "ThemeConfig parsed from wie2026_theme.json produces WiE 2026 colors"
    - "Swapping JSON file changes theme colors with no code changes"
    - "All data model classes compile and are importable"
  artifacts:
    - path: "app/build.gradle.kts"
      provides: "Compose + MVVM dependencies, Kotlin plugin"
      contains: "compose-bom"
    - path: "app/src/main/java/com/gow/smaitrobot/data/model/ThemeConfig.kt"
      provides: "Theme data classes with defaults"
      exports: ["ThemeConfig", "ThemeColors", "CardConfig", "SponsorConfig"]
    - path: "app/src/main/java/com/gow/smaitrobot/data/theme/ThemeRepository.kt"
      provides: "JSON asset loading into ThemeConfig StateFlow"
      exports: ["ThemeRepository"]
    - path: "app/src/main/java/com/gow/smaitrobot/ui/theme/AppTheme.kt"
      provides: "MaterialTheme wrapper reading ThemeConfig"
      exports: ["AppTheme"]
    - path: "app/src/main/assets/wie2026_theme.json"
      provides: "WiE 2026 event configuration"
      contains: "Engineering Beyond Imagination"
  key_links:
    - from: "ThemeRepository"
      to: "wie2026_theme.json"
      via: "context.assets.open() + Gson"
      pattern: "Gson.*fromJson.*ThemeConfig"
    - from: "AppTheme"
      to: "ThemeConfig"
      via: "lightColorScheme from parsed colors"
      pattern: "lightColorScheme.*primary.*secondary"
---

<objective>
Migrate the Android app build system to Jetpack Compose + Kotlin, create the runtime JSON theming system, define all data models, and populate the WiE 2026 theme asset.

Purpose: Establishes the build foundation and design system that all subsequent screens depend on. Without Compose dependencies and the theme system, no UI can be built.

Output: Compilable Compose project with theme system loading WiE 2026 branding from JSON, all shared data models defined.
</objective>

<execution_context>
@/home/gow/.claude/get-shit-done/workflows/execute-plan.md
@/home/gow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-android-app-rebuild-and-wie-theme-home/12-CONTEXT.md
@.planning/phases/12-android-app-rebuild-and-wie-theme-home/12-RESEARCH.md

Source codebase: /home/gow/.openclaw/workspace/projects/smait-jackie-app

<interfaces>
<!-- Current build.gradle.kts uses AGP 9.0.0, no Kotlin plugin, no Compose -->
<!-- Current libs.versions.toml has no Compose/Kotlin entries -->
<!-- Existing classes to preserve: CaeAudioManager.kt, TtsAudioPlayer.kt, their tests -->
<!-- Local JARs: libs/cae.jar, libs/AlsaRecorder.jar -->
<!-- ABI filter: armeabi-v7a (do NOT change) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate build system to Kotlin + Jetpack Compose</name>
  <files>
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/gradle/libs.versions.toml
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/settings.gradle.kts
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/build.gradle.kts
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/build.gradle.kts
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/AndroidManifest.xml
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/JackieApplication.kt
  </files>
  <behavior>
    - Test 1: `./gradlew :app:assembleDebug` succeeds (Compose project compiles)
    - Test 2: Existing CaeAudioManagerTest and TtsAudioPlayerTest still pass
  </behavior>
  <action>
    Update the build system to support Jetpack Compose with Kotlin:

    1. **libs.versions.toml** — Add versions and libraries:
       - kotlin = "2.0.21" (or latest 2.0.x stable)
       - compose-bom = "2026.03.00" (if unavailable, use latest stable BOM like 2025.01.01)
       - navigation-compose = "2.8.9"
       - lottie-compose = "6.6.0"
       - okhttp = "4.12.0" (already used)
       - gson = "2.11.0"
       - kotlinx-serialization = "1.7.3"
       - lifecycle = "2.8.7"
       - activity-compose = "1.9.3"
       - turbine = "1.2.0" (testImplementation)
       - coroutines = "1.8.1"
       - Add kotlin plugin: org.jetbrains.kotlin.android
       - Add compose-compiler plugin: org.jetbrains.kotlin.plugin.compose
       - Add kotlinx-serialization plugin: org.jetbrains.kotlin.plugin.serialization

    2. **Root build.gradle.kts** — Add kotlin and compose-compiler plugins in plugins block (apply false).

    3. **settings.gradle.kts** — No changes needed if plugin repos already include mavenCentral + google.

    4. **app/build.gradle.kts** — Major updates:
       - Apply kotlin-android, compose-compiler, kotlinx-serialization plugins
       - Add `buildFeatures { compose = true }` — NO `composeOptions` block (Kotlin 2.0+ handles this automatically)
       - Keep `ndk { abiFilters += "armeabi-v7a" }` EXACTLY as-is
       - Keep `implementation(files("libs/cae.jar"))` and `implementation(files("libs/AlsaRecorder.jar"))`
       - Add Compose BOM platform dependency
       - Add: compose-material3, compose-ui, compose-ui-tooling-preview, navigation-compose, lottie-compose, gson, kotlinx-serialization-json
       - Add: lifecycle-viewmodel-compose, lifecycle-runtime-compose, activity-compose
       - Add: coroutines-android
       - testImplementation: turbine
       - Keep existing test dependencies (junit, espresso, etc.)
       - Change `compileOptions` to Java 17 (Compose requires Java 17+)
       - Add `kotlinOptions { jvmTarget = "17" }`

    5. **AndroidManifest.xml** — Add `android:name=".JackieApplication"` to application tag. Keep all existing permissions (CAMERA, RECORD_AUDIO, INTERNET, etc.).

    6. **JackieApplication.kt** — Create minimal Application subclass. This will later hold singleton instances (WebSocketRepository, ThemeRepository). For now, just `class JackieApplication : Application()`.

    IMPORTANT: Do NOT delete or modify CaeAudioManager.kt, TtsAudioPlayer.kt, or MainActivity.kt. They must still compile. If they reference Java-only APIs, they will compile fine with Kotlin plugin added.

    IMPORTANT: The existing app does NOT use Kotlin plugin currently (only android-application plugin). The AGP 9.0.0 is very recent. Verify Kotlin plugin compatibility — if AGP 9.0.0 requires Kotlin 2.1.x, use that version instead.
  </action>
  <verify>
    <automated>cd /home/gow/.openclaw/workspace/projects/smait-jackie-app && ./gradlew :app:assembleDebug 2>&1 | tail -20</automated>
  </verify>
  <done>App builds with Compose dependencies, existing tests pass, JackieApplication registered in manifest</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create theme system, data models, and WiE 2026 JSON config</name>
  <files>
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/data/model/ThemeConfig.kt
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/data/model/ChatMessage.kt
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/data/model/NavStatus.kt
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/data/model/PoiItem.kt
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/data/model/FeedbackData.kt
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/data/theme/ThemeRepository.kt
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/ui/theme/AppTheme.kt
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/ui/theme/WiEColors.kt
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/assets/wie2026_theme.json
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/assets/default_theme.json
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/test/java/com/gow/smaitrobot/ThemeRepositoryTest.kt
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/test/java/com/gow/smaitrobot/WiEThemeTest.kt
  </files>
  <behavior>
    - Test 1: ThemeConfig.default() returns valid config with non-empty eventName and valid hex colors
    - Test 2: Gson parses wie2026_theme.json into ThemeConfig with eventName "WiE 2026"
    - Test 3: WiE theme primary color is "#7B2D8B" (purple)
    - Test 4: WiE theme has 6 cards in cards[] array
    - Test 5: WiE theme tagline is "Engineering Beyond Imagination"
    - Test 6: ThemeConfig with missing fields falls back to defaults (no NPE)
    - Test 7: default_theme.json parses with different colors than WiE (swap verification)
    - Test 8: ChatMessage has id, text, isUser, timestamp fields
    - Test 9: NavStatus has destination, status, progress fields
  </behavior>
  <action>
    Create the complete data model layer and theme system:

    1. **ThemeConfig.kt** — Data classes with Gson annotations and safe defaults:
       - `ThemeConfig(eventName, tagline, logoAsset, colors: ThemeColors, fonts: ThemeFonts, sponsors: List<SponsorConfig>, cards: List<CardConfig>, schedule: List<ScheduleItem>, speakers: List<SpeakerInfo>)` — ALL fields have defaults via `ThemeConfig.default()`
       - `ThemeColors(primary="#7B2D8B", secondary="#00A99D", tertiary="#F7941D", background="#FAFAFA", onPrimary="#FFFFFF", onBackground="#1C1B1F", surface="#FFFFFF", onSurface="#1C1B1F")`
       - `CardConfig(label, action, icon, description="")` — action is string like "navigate:chat" or "inline:keynote"
       - `SponsorConfig(name, logoAsset)`
       - `ScheduleItem(time, title, speaker, location, track="")`
       - `SpeakerInfo(name, title, bio, photoAsset="")`

    2. **ChatMessage.kt** — `data class ChatMessage(val id: String, val text: String, val isUser: Boolean, val timestamp: Long = System.currentTimeMillis())`

    3. **NavStatus.kt** — `data class NavStatus(val destination: String, val status: String, val progress: Float = 0f)` where status is "navigating", "arrived", "failed", "cancelled"

    4. **PoiItem.kt** — `data class PoiItem(val name: String, val humanName: String, val category: String = "", val floor: String = "")`

    5. **FeedbackData.kt** — `data class FeedbackData(val rating: Int, val surveyResponses: Map<String, String> = emptyMap(), val sessionId: String, val timestamp: Long = System.currentTimeMillis())`

    6. **ThemeRepository.kt** — Loads theme JSON from assets:
       - Constructor takes `Context` (or `AssetManager`)
       - `private val _config = MutableStateFlow(ThemeConfig.default())`
       - `val config: StateFlow<ThemeConfig>`
       - `fun load(assetFileName: String = "wie2026_theme.json")` — reads JSON via `context.assets.open()`, parses with Gson, wraps in try/catch with fallback to `ThemeConfig.default()` on any exception
       - Load on IO dispatcher (caller responsibility or internal `withContext(Dispatchers.IO)`)

    7. **WiEColors.kt** — Object with WiE 2026 color constants as hex strings:
       - `const val PRIMARY = "#7B2D8B"` (purple)
       - `const val SECONDARY = "#00A99D"` (teal)
       - `const val TERTIARY = "#F7941D"` (orange)
       - Plus background, surface, onPrimary, etc.

    8. **AppTheme.kt** — `@Composable fun AppTheme(config: ThemeConfig, content: @Composable () -> Unit)`:
       - Builds `lightColorScheme()` from `config.colors` using `Color(android.graphics.Color.parseColor(hex))`
       - Wraps content in `MaterialTheme(colorScheme, typography, content)`
       - Typography uses Material 3 defaults (fonts loaded via system — Roboto default)

    9. **wie2026_theme.json** — Full WiE 2026 config per RESEARCH.md pattern:
       - eventName: "WiE 2026"
       - tagline: "Engineering Beyond Imagination"
       - WiE purple/teal/orange palette
       - 6 cards: "Ask Me Anything" (navigate:chat), "Guided Tour" (navigate:map), "Keynote Info" (inline:keynote), "Session Tracks" (inline:sessions), "Facilities" (navigate:facilities), "Event Info" (navigate:eventinfo)
       - sponsors array with SJSU Engineering, BioRob Lab entries
       - schedule with 3-4 sample WiE session entries
       - speakers with 2-3 placeholder entries

    10. **default_theme.json** — Minimal default with different colors (blue/gray) to verify theme swapping works

    11. **Tests** — ThemeRepositoryTest and WiEThemeTest as described in behavior block. For ThemeRepository tests, either mock AssetManager or use Robolectric to access test assets. If Robolectric setup is complex, test ThemeConfig parsing directly with Gson from a string literal (simpler, still validates the contract).
  </action>
  <verify>
    <automated>cd /home/gow/.openclaw/workspace/projects/smait-jackie-app && ./gradlew :app:testDebugUnitTest --tests "com.gow.smaitrobot.ThemeRepositoryTest" --tests "com.gow.smaitrobot.WiEThemeTest" 2>&1 | tail -20</automated>
  </verify>
  <done>ThemeConfig parses WiE JSON with correct colors/cards/tagline, default fallback works, all data models compile, AppTheme composable builds ColorScheme from config</done>
</task>

</tasks>

<verification>
1. `cd /home/gow/.openclaw/workspace/projects/smait-jackie-app && ./gradlew :app:assembleDebug` succeeds
2. `./gradlew :app:testDebugUnitTest` passes all tests including existing CaeAudioManager and TtsAudioPlayer tests
3. ThemeRepository and WiETheme tests pass
</verification>

<success_criteria>
- App compiles with Jetpack Compose + Material 3 + all new dependencies
- Existing CaeAudioManager and TtsAudioPlayer code unchanged and tests passing
- wie2026_theme.json contains WiE 2026 branding (purple/teal/orange, "Engineering Beyond Imagination", 6 cards)
- ThemeRepository loads JSON and exposes StateFlow<ThemeConfig>
- AppTheme composable builds Material 3 ColorScheme from ThemeConfig
- All data models (ChatMessage, NavStatus, PoiItem, FeedbackData) defined
</success_criteria>

<output>
After completion, create `.planning/phases/12-android-app-rebuild-and-wie-theme-home/12-01-SUMMARY.md`
</output>
