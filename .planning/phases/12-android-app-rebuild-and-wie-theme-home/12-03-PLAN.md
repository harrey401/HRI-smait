---
phase: 12-android-app-rebuild-and-wie-theme-home
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/ui/home/HomeScreen.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/ui/home/HomeViewModel.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/ui/common/SponsorBar.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/ui/common/TopLogoBar.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/ui/eventinfo/EventInfoScreen.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/ui/eventinfo/EventInfoViewModel.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/navigation/AppNavigation.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/test/java/com/gow/smaitrobot/HomeViewModelTest.kt
  - /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/test/java/com/gow/smaitrobot/EventInfoViewModelTest.kt
autonomous: true
requirements: [APP-03, APP-07, WIE-02]

must_haves:
  truths:
    - "Home screen displays 6 WiE cards in a grid layout"
    - "Tapping a card with action 'navigate:chat' switches to Chat tab"
    - "SJSU + BioRob logos appear at top of Home screen"
    - "Sponsor bar with logos appears at bottom of Home and Event Info screens"
    - "Event Info screen shows schedule, speakers from theme config"
  artifacts:
    - path: "app/src/main/java/com/gow/smaitrobot/ui/home/HomeScreen.kt"
      provides: "Home screen with card grid, logo bar, sponsor bar"
      min_lines: 80
    - path: "app/src/main/java/com/gow/smaitrobot/ui/eventinfo/EventInfoScreen.kt"
      provides: "Event info screen with schedule, speakers, venue map"
      min_lines: 60
    - path: "app/src/main/java/com/gow/smaitrobot/ui/common/SponsorBar.kt"
      provides: "Reusable sponsor bar with auto-scroll"
      min_lines: 30
  key_links:
    - from: "HomeViewModel"
      to: "ThemeRepository"
      via: "config StateFlow collection"
      pattern: "themeRepository\\.config"
    - from: "HomeScreen card tap"
      to: "navController.navigate"
      via: "CardConfig.action parsing"
      pattern: "navigate:chat|navigate:map"
---

<objective>
Build the Home screen (card grid with WiE branding) and Event Info screen (schedule/speakers). These are the two branding-heavy screens that show sponsor bars and event-specific content from the JSON theme config.

Purpose: Home screen is the idle/welcome view that users first interact with. Event Info provides conference details. Both are JSON-config-driven per user decision.

Output: Fully functional Home and Event Info screens with WiE 2026 content.
</objective>

<execution_context>
@/home/gow/.claude/get-shit-done/workflows/execute-plan.md
@/home/gow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-android-app-rebuild-and-wie-theme-home/12-CONTEXT.md
@.planning/phases/12-android-app-rebuild-and-wie-theme-home/12-RESEARCH.md
@.planning/phases/12-android-app-rebuild-and-wie-theme-home/12-01-SUMMARY.md
@.planning/phases/12-android-app-rebuild-and-wie-theme-home/12-02-SUMMARY.md

Source codebase: /home/gow/.openclaw/workspace/projects/smait-jackie-app

<interfaces>
<!-- From Plan 01: -->
<!-- ThemeConfig(eventName, tagline, logoAsset, colors, fonts, sponsors, cards, schedule, speakers) -->
<!-- CardConfig(label, action, icon, description) — action: "navigate:chat", "inline:keynote", etc. -->
<!-- SponsorConfig(name, logoAsset) -->
<!-- ScheduleItem(time, title, speaker, location, track) -->
<!-- SpeakerInfo(name, title, bio, photoAsset) -->
<!-- ThemeRepository.config: StateFlow<ThemeConfig> -->
<!-- AppTheme composable wraps MaterialTheme with JSON-derived colors -->

<!-- From Plan 02: -->
<!-- Screen sealed class: Home, Chat, Map, Facilities, EventInfo -->
<!-- AppScaffold with NavHost and BottomNavigationBar -->
<!-- JackieApplication.themeRepository available -->
<!-- NavController for programmatic navigation -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Home screen with card grid, logos, and sponsor bar</name>
  <files>
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/ui/home/HomeScreen.kt
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/ui/home/HomeViewModel.kt
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/ui/common/SponsorBar.kt
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/ui/common/TopLogoBar.kt
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/test/java/com/gow/smaitrobot/HomeViewModelTest.kt
  </files>
  <behavior>
    - Test 1: HomeViewModel.cards returns cards from ThemeConfig (6 cards for WiE)
    - Test 2: HomeViewModel.eventName returns ThemeConfig.eventName
    - Test 3: HomeViewModel.sponsors returns ThemeConfig.sponsors list
    - Test 4: parseCardAction("navigate:chat") returns NavigateToTab(Screen.Chat)
    - Test 5: parseCardAction("inline:keynote") returns ShowInlineContent("keynote")
  </behavior>
  <action>
    Build the Home screen per user decisions:

    1. **HomeViewModel.kt**:
       - Takes ThemeRepository as constructor parameter
       - Exposes `val cards: StateFlow<List<CardConfig>>` — mapped from themeRepository.config
       - Exposes `val eventName: StateFlow<String>`, `val tagline: StateFlow<String>`
       - Exposes `val sponsors: StateFlow<List<SponsorConfig>>`
       - `fun parseCardAction(action: String): CardAction` — sealed class:
         - `NavigateToTab(screen: Screen)` for "navigate:chat", "navigate:map", "navigate:facilities", "navigate:eventinfo"
         - `ShowInlineContent(contentKey: String)` for "inline:keynote", "inline:sessions"

    2. **HomeScreen.kt** — `@Composable fun HomeScreen(viewModel: HomeViewModel, navController: NavHostController)`:
       - Layout (top to bottom in a Column):
         a. **TopLogoBar** — SJSU + BioRob lab logos horizontally centered. Use placeholder drawable resources (can be replaced with real PNGs later). 60dp height.
         b. **Event name + tagline** — Centered text, eventName in 28sp bold (primary color), tagline in 18sp below
         c. **Card grid** — `LazyVerticalGrid(columns = GridCells.Fixed(3))` for 2x3 layout (or GridCells.Fixed(2) for 2x4 if more than 6 cards)
           - Each card: `ElevatedCard` with Material 3 styling
           - Card content: Icon (Material Icons mapped from CardConfig.icon string) + label text below
           - Large touch targets: 80dp minimum card height, 5mm+ padding (8dp) between cards
           - Visual tap feedback: ripple via `clickable` modifier (default Material behavior)
           - 18sp minimum text, 24sp for card labels
           - High contrast: card on surface color, text on onSurface
           - On tap: parse action → if NavigateToTab, call `navController.navigate(screen)` via callback; if ShowInlineContent, show bottom sheet or dialog with content
         d. **SponsorBar** at bottom

       - Icon mapping helper: map CardConfig.icon string ("chat", "map", "star", "schedule", "location", "info") to Material Icons (Icons.Filled.Chat, Icons.Filled.Map, etc.)

    3. **TopLogoBar.kt** — `@Composable fun TopLogoBar()`:
       - Row with two placeholder Image composables (SJSU logo, BioRob logo)
       - Horizontally centered, 60dp height
       - For now, use Box with Text("SJSU") and Text("BioRob") as placeholders until real logo PNGs are added

    4. **SponsorBar.kt** — `@Composable fun SponsorBar(sponsors: List<SponsorConfig>)`:
       - If sponsors fit in viewport: static Row with logos
       - If sponsors overflow: slow horizontal auto-scroll using `LaunchedEffect` + `ScrollState` animation (50ms delay, 1px per tick, loop back to 0)
       - 48dp height, horizontal padding 16dp
       - Logos as placeholder Text composables until real assets added
       - Per user decision: appears on Home and Event Info screens ONLY

    5. **Update AppNavigation.kt** — Replace Home placeholder with `HomeScreen(viewModel, navController)`. Pass ThemeRepository to HomeViewModel factory.
  </action>
  <verify>
    <automated>cd /home/gow/.openclaw/workspace/projects/smait-jackie-app && ./gradlew :app:testDebugUnitTest --tests "com.gow.smaitrobot.HomeViewModelTest" 2>&1 | tail -20</automated>
  </verify>
  <done>Home screen renders card grid from JSON config, cards navigate to correct tabs, sponsor bar auto-scrolls, SJSU+BioRob logo bar at top</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Event Info screen with schedule, speakers, and venue info</name>
  <files>
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/ui/eventinfo/EventInfoScreen.kt
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/ui/eventinfo/EventInfoViewModel.kt
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/test/java/com/gow/smaitrobot/EventInfoViewModelTest.kt
    /home/gow/.openclaw/workspace/projects/smait-jackie-app/app/src/main/java/com/gow/smaitrobot/navigation/AppNavigation.kt
  </files>
  <behavior>
    - Test 1: EventInfoViewModel.schedule returns schedule items from ThemeConfig
    - Test 2: EventInfoViewModel.speakers returns speakers from ThemeConfig
    - Test 3: EventInfoViewModel.eventName returns ThemeConfig.eventName
    - Test 4: Empty schedule list results in empty state (no crash)
  </behavior>
  <action>
    Build the Event Info screen:

    1. **EventInfoViewModel.kt**:
       - Takes ThemeRepository
       - Exposes `val schedule: StateFlow<List<ScheduleItem>>` from config
       - Exposes `val speakers: StateFlow<List<SpeakerInfo>>` from config
       - Exposes `val eventName: StateFlow<String>`, `val tagline: StateFlow<String>`
       - Exposes `val sponsors: StateFlow<List<SponsorConfig>>` (for SponsorBar)

    2. **EventInfoScreen.kt** — `@Composable fun EventInfoScreen(viewModel: EventInfoViewModel)`:
       - Scrollable Column layout:
         a. **Event header** — Event name (28sp bold) + tagline (18sp)
         b. **Schedule section** — "Schedule" heading (24sp), then LazyColumn of schedule items:
            - Each item: Card with time (bold), title, speaker name, location, track badge
            - Grouped by track if multiple tracks exist
         c. **Speakers section** — "Speakers" heading (24sp), then horizontal LazyRow of speaker cards:
            - Each card: placeholder avatar circle, name (bold), title, short bio
         d. **Venue info** — Placeholder section for venue map image (will show map PNG from config if available)
         e. **SponsorBar** at bottom (reuse from common/)

       - Accessibility: 18sp minimum text, high contrast, large touch targets on any interactive elements
       - Use `collectAsStateWithLifecycle()` for all StateFlow collections

    3. **Update AppNavigation.kt** — Replace EventInfo placeholder with `EventInfoScreen(viewModel)`.
  </action>
  <verify>
    <automated>cd /home/gow/.openclaw/workspace/projects/smait-jackie-app && ./gradlew :app:testDebugUnitTest --tests "com.gow.smaitrobot.EventInfoViewModelTest" 2>&1 | tail -20</automated>
  </verify>
  <done>Event Info screen shows WiE schedule and speakers from JSON config, SponsorBar at bottom, accessible text sizing</done>
</task>

</tasks>

<verification>
1. `./gradlew :app:assembleDebug` succeeds
2. HomeViewModelTest and EventInfoViewModelTest pass
3. Home screen card grid renders 6 cards from WiE config
4. Tapping "Ask Me Anything" card navigates to Chat tab
</verification>

<success_criteria>
- Home screen shows SJSU+BioRob logos, event name/tagline, 6 WiE cards in grid, sponsor bar
- Card taps navigate to correct bottom nav destinations or show inline content
- Event Info screen shows schedule, speakers, venue info from JSON config
- Sponsor bar appears on Home and Event Info only (not other screens)
- All text 18sp+ body, 24sp+ headings, 60-80px touch targets, ripple feedback
</success_criteria>

<output>
After completion, create `.planning/phases/12-android-app-rebuild-and-wie-theme-home/12-03-SUMMARY.md`
</output>
